{
  "name": "torcons-extension",
  "version": "1.7.0",
  "description": "AI Assistant to summarize pages and answer questions.",
  "scripts": {
    "dev": "web-ext run --target=chromium",
    "build": "web-ext build"
  },
  "devDependencies": {
    "web-ext": "^10.5.0"
  }
}
